# Data Frames
#-------------------------------


#df of vectors
df    <- data.frame(
                      a=c(1:5),
                      b=(-1:-5),
                      c=(11:15),
                      d=(-16:-20))
                   list(-1,-2,-3))

# Creating Data Frames
#=================================

# Accessing Data Frames
#=================================
# indexing: [ ]
# specifying
# colnames (accessing,setting,changing)
# push/pop

# selecting with new col  names:
# - colnames(df) <- c("id", "nobs")   -or-
# - df[566:570,c('sequence','start','end')]
